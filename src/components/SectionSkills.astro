---
const { class: className, id } = Astro.props
---

<section
    id={id}
    class={`bg-blue-950 h-[2000px] lm:h-screen md:h-[700px] mx-auto flex flex-col justify-center items-center ${className}`}
>
    <slot />
</section>
