---
const { class: className, id } = Astro.props
---

<section
id={id}
class={`w-full max-w-[800px] mx-auto h-screen flex flex-col justify-center items-center ${className}`}
>
    <slot />
</section>
