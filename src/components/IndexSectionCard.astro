---
interface Props {
    title: string
    paragraph: string
}

const { title, paragraph } = Astro.props
---

<div class="flex flex-col text-center text-3xl gap-4">
    <h1 class="underline">{title}</h1>
    <p>{paragraph}</p>
</div>
